import { PolicyPage } from "@/components/legal/PolicyPage";

export const metadata = { title: "Return / Refund Policy | ClamCart" };

export default function ReturnRefundPolicyPage() {
  return <PolicyPage title="Return / Refund Policy" description="Add the store’s actual return eligibility, return window, refund process, exclusions, and customer contact process." />;
}
