import { CheckoutClient } from "@/components/cart/CheckoutClient";

export const metadata = {
  title: "Checkout | ClamCart",
};

export default function CheckoutPage() {
  return <CheckoutClient />;
}
